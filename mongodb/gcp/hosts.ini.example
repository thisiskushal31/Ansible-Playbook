# Sample inventory for GCE MongoDB hosts. Replace with your primary/secondary IPs or hostnames.
# Can be generated by your pipeline from Terraform output or a dynamic inventory.

[primary]
# 10.140.0.26

[secondary]
# 10.140.0.29
# 10.140.0.32

[all:vars]
# Override if not passed as -e
# remote_user=ubuntu
# mongo_version=6.0
# mongo_repl_set=rs0
# gcp_project=your-gcp-project-id
# mongo_key_auth_secret=your-keyfile-secret-name
